################################################################
## Packages / Presence
################################################################

################################################
## Customize
################################################

homeassistant:
  customize:
    ################################################
    ## Node Anchors
    ################################################

    package.node_anchors:
      customize: &customize
        package: 'presence'

      expose: &expose
        <<: *customize
        homebridge_hidden: true

    ################################################
    ## Automation
    ################################################

    automation.person_just_arrived:
      <<: *expose
      friendly_name: "Mark person as just arrived"
      icon: mdi:map-marker

    automation.person_home:
      <<: *customize
      friendly_name: "Mark person as home"
      icon: mdi:map-marker

    automation.person_just_left:
      <<: *customize
      friendly_name: "Mark person as just left"
      icon: mdi:map-marker

    automation.person_away:
      <<: *customize
      friendly_name: "Mark person as away"
      icon: mdi:map-marker

    automation.person_extended_away:
      <<: *customize
      friendly_name: "Mark person as extended away"
      icon: mdi:map-marker

    ################################################
    ## Input Select
    ################################################

    input_select.julias_status:
      <<: *customize
      friendly_name: "Julia"
      entity_picture: /local/julia.jpg

    input_select.michaels_status:
      <<: *customize
      friendly_name: "Michael"
      entity_picture: /local/michael.jpg

    ################################################
    ## Sensor
    ################################################

    sensor.household:
      <<: *customize
      friendly_name: "Household"

################################################
## Input Select
################################################

input_select:
  julias_status:
    options:
      - Home
      - Just Arrived
      - Just Left
      - Away
      - Extended Away

  michaels_status:
    options:
      - Home
      - Just Arrived
      - Just Left
      - Away
      - Extended Away

################################################
## Sensor
################################################

sensor:
  - platform: template
    sensors:
      household:
        entity_id:
          - input_select.julias_status
          - input_select.michaels_status
        value_template: >-
          {% if is_state('input_select.julias_status', 'Home') or is_state('input_select.michaels_status', 'Home') %}
            Home
          {% elif is_state('input_select.julias_status', 'Just Arrived') or is_state('input_select.michaels_status', 'Just Arrived') %}
            Arrival
          {% elif is_state('input_select.julias_status', 'Extended Away') and is_state('input_select.michaels_status', 'Extended Away') %}
            Vacation
          {% else %}
            Away
          {% endif %}
        icon_template: >-
          {% set household_status = states('sensor.household') %}
          {% if household_status == 'Home' or household_status == 'Arrival' %}
            mdi:home
          {% elif household_status == 'Extended Away' %}
            mdi:beach
          {% else %}
            mdi:home-outline
          {% endif %}

################################################
## Automation
################################################

automation:
  - alias: person_just_arrived
    hide_entity: true
    initial_state: 'on'
    trigger:
      - platform: state
        entity_id: device_tracker.julias_iphone
        from: 'not_home'
        to: 'home'
      - platform: state
        entity_id: device_tracker.michaels_iphone
        from: 'not_home'
        to: 'home'
    action:
      - service: input_select.select_option
        data_template:
          entity_id: >-
            {% if trigger.entity_id == 'device_tracker.julias_iphone' %}
              input_select.julias_status
            {% else %}
              input_select.michaels_status
            {% endif %}
          option: >-
            {% if trigger.entity_id == 'device_tracker.julias_iphone' %}
              {% if states.input_select.julias_status.state == 'Just Left' %}
                Home
              {% else %}
                Just Arrived
              {% endif %}
            {% else %}
              {% if states.input_select.michaels_status.state == 'Just Left' %}
                Home
              {% else %}
                Just Arrived
              {% endif %}
            {% endif %}
 
  - alias: person_home
    hide_entity: true
    initial_state: 'on'
    trigger:
      - platform: state
        entity_id: input_select.julias_status
        to: 'Just Arrived'
        for:
          minutes: 10
      - platform: state
        entity_id: input_select.michaels_status
        to: 'Just Arrived'
        for:
          minutes: 10
      - platform: state
        entity_id: input_select.julias_status
        from: 'Just Left'
        to: 'Just Arrived'
      - platform: state
        entity_id: input_select.michaels_status
        from: 'Just Left'
        to: 'Just Arrived'
    action:
      - service: input_select.select_option
        data_template:
          entity_id: >
            {% if trigger.entity_id == 'input_select.julias_status' %}
              input_select.julias_status
            {% else %}
              input_select.michaels_status
            {% endif %}
          option: Home
 
  - alias: person_just_left
    hide_entity: true
    initial_state: 'on'
    trigger:
      - platform: state
        entity_id: device_tracker.julias_iphone
        from: 'home'
        to: 'not_home'
      - platform: state
        entity_id: device_tracker.michaels_iphone
        from: 'home'
        to: 'not_home'
    action:
      - service: input_select.select_option
        data_template:
          entity_id: >
            {% if trigger.entity_id == 'device_tracker.julias_iphone' %}
              input_select.julias_status
            {% else %}
              input_select.michaels_status
            {% endif %}
          option: Just Left
 
  - alias: person_away
    hide_entity: true
    initial_state: 'on'
    trigger:
      - platform: state
        entity_id: input_select.julias_status
        to: 'Just Left'
        for:
          minutes: 10
      - platform: state
        entity_id: input_select.michaels_status
        to: 'Just Left'
        for:
          minutes: 10
    action:
      - service: input_select.select_option
        data_template:
          entity_id: >
            {% if trigger.entity_id == 'input_select.julias_status' %}
              input_select.julias_status
            {% else %}
              input_select.michaels_status
            {% endif %}
          option: Away
 
  - alias: person_extended_away
    hide_entity: true
    initial_state: 'on'
    trigger:
      - platform: state
        entity_id: input_select.julias_status
        to: 'Away'
        for:
          hours: 24
      - platform: state
        entity_id: input_select.michaels_status
        to: 'Away'
        for:
          hours: 24
    action:
      - service: input_select.select_option
        data_template:
          entity_id: >
            {% if trigger.entity_id == 'input_select.julias_status' %}
              input_select.julias_status
            {% else %}
              input_select.michaels_status
            {% endif %}
          option: Extended Away

################################################
## Device Tracker
################################################

# Device Tracking
# https://www.home-assistant.io/components/device_tracker.bluetooth_tracker/
device_tracker:
  - platform: bluetooth_tracker
    new_device_defaults:
      track_new_devices: false
      hide_if_away: false
