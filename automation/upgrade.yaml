### automation/upgrade.yaml

# Send a notification when and update is available
alias: 'Update Available Notifications'
trigger:
  platform: state
  entity_id: updater.updater
action:
  - service: notify.twilio
    data:
      message: 'Installing update for Home Assistant.'
      target: !secret mobile_michael
  - service: shell_command.upgrade
